{
  "route1": {
    "pattern": "/test/{name}/coucou/{number}",
    "method": "get",
    "parameters": {
      "name" : "/[a-Z]+/",
      "number": "[0-9]+"
    },
    "controller": "Main::home"
  },

  "route2": {
    "pattern": "/hello",
    "method": "get",
    "parameters": {
      "name" : "/[a-Z]+/",
      "number": "[0-9]+"
    },
    "controller": "Main::home"
  },

  "route3": {
    "pattern": "/hello/world",
    "method": "post",
    "parameters": {
      "name" : "/[a-Z]+/",
      "number": "[0-9]+"
    }
  }
}